let x = [12, 12]

let  y = [true, false, true]

let z = ["A", "B", "C"]


🌶 add (list: Number) Number
    let sum  = 0
    while (i > 2)
      sum = sum + list[i]
    end
    return sum
  end

add(x)
